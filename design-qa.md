# Credit 风格 UI 复核

final result: passed

## 视觉来源与状态

- 参考 URL：https://credit.linux.do/home（未登录访问跳转登录页；以用户提供的两张已登录截图作为视觉真值）。
- 首页来源：`/var/folders/qs/5k8x1q_153d79q8j0bhgwl440000gn/T/codex-clipboard-c5829104-5df6-4656-9421-98326b36b546.png`，2549×1183 像素。
- 指南来源：`/var/folders/qs/5k8x1q_153d79q8j0bhgwl440000gn/T/codex-clipboard-dbdac1d6-34b8-4af2-b241-41b035420d31.png`，2560×1205 像素。
- 云端使用真实应用与加密的隔离测试账本（3 台设备、540 个聚合桶），并非用户账本；本地使用独立临时 home 下的 7 个 Codex 会话。发布包不包含测试入口或这些账本。
- 风格参考而非业务页面复制：保留 UsageMesh 的筛选、四个核心指标、账本下界标识和自有品牌；不复制积分、争议等无关业务。

## 视口与比较证据

- 首页 CSS 视口 2549×1183，最终捕获 `docs/qa/credit-ui/cloud-desktop-final.png` 为 2543×1180。因宿主截图缩放存在约 0.3% 差异，比较按共同显示宽度归一化，不报告像素级误差。
- 指南 CSS 视口 2560×1205，捕获 `docs/qa/credit-ui/cloud-guide.png` 为 2549×1183。源图与实现同为浅色、顶部、目录展开；同宽归一化比较。保留全局侧栏，所以内容中心位于剩余工作区内。
- 本地桌面 CSS 1440×1000，`docs/qa/credit-ui/local-desktop.png` 与 `local-guide.png`；移动 CSS 390×844，`local-mobile.png`。
- 云端移动 CSS 390×844：`cloud-mobile.png`（指南）、`cloud-mobile-overview.png`（筛选后）、`cloud-mobile-dark.png`（稳定深色状态）。
- 首页和指南均将用户源图与实现截图放在同一次图像比较输入中查看。源图的大区结构与文字层级可直接辨认；另检查 1440px 桌面和 390px 手机截图中的正文、控件、换行、间距。此次目标为风格重构，无需对品牌或图表数据做局部像素匹配。

## 迭代记录

1. [P2，已修复] 首页筛选和摘要占高过多，近期概览出现过晚。证据：`cloud-desktop.png`。修复：详细筛选与保存视图收进展开区；压缩摘要间距和冗余注释；缩短图表高度。重新截图并与首页源图同输入比较：`cloud-desktop-final.png`。
2. [P2，已修复] 本地指南有重复页标题。移除重复标题的视觉占位，保留可访问标题；最终证据：`local-guide.png`。
3. 深色模式初次截图命中颜色过渡，重新在稳定状态捕获；按钮背景 #111113、文字 #fafafa，最终证据：`cloud-mobile-dark.png`。

## 五项表面检查

- 字体：系统 UI 与中文字体栈，无远程字体依赖。正文、标签、标题分级，避免过细字重；保留大字号选项。为可读性保留比参考图更清楚的导航字重。
- 布局：224px 窄侧栏、内容居中、趋势/摘要双栏、浅灰背景上的三列概览卡片；移动端改为单列或双列摘要。指南目录与章节分栏，窄屏可访问。390px 下云端 scrollWidth 384、本地 375，均未超过 innerWidth 390。
- 颜色：白/浅灰主表面、淡紫选中态、蓝色趋势、细分隔线；深色模式沿用独立变量。费用、同步和错误状态仍保留语义文字。
- 图像：没有照片/装饰插画需求。保留 UsageMesh 品牌；图标来自现有 Lucide 库，本地图标随二进制内嵌。图表来自实际数据，不把参考图曲线伪装成用量。
- 内容：近期活动、Token 构成与排名沿用真实筛选结果；费用继续注明估算及下界。内置指南说明实际操作和本地/云端边界，无设计提示词泄漏。

## 功能与实现检查

- 浏览器验证了设备筛选（540→180 桶）、收起后保留条件、保存视图入口、返回概览、深色模式、本地扫描（7 个会话、1 个项目）、本地搜索和清空恢复导航、指南章节跳转不改写应用路由。
- 新增回归测试覆盖展开/收起筛选与指南路由保护；49 项前端测试、65 项 Rust 测试通过；类型检查、格式检查、生产构建通过。
- 浏览器响应式截图均来自可交互应用，不使用静态页面替代。
- 无待修复 P0/P1/P2。参考未提供移动端和深色版，按现有产品能力作一致性适配；不声称这些状态与参考逐像素一致。

## 完成清单

- [x] 云端与本地界面统一
- [x] 首页与指南视觉复核和迭代
- [x] 筛选、搜索、导航及移动端验证
- [x] 发布前移除隔离验证入口和账本
