name: Fix v2.2.3 accounting migration

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-v223.yml'
      - '.github/scripts/fix_v223.py'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply patch
        run: python3 .github/scripts/fix_v223.py
      - name: Verify Rust
        shell: bash
        run: |
          cargo fmt --all -- --check || cargo fmt --all
          cargo test --workspace --all-targets
      - name: Verify dashboard
        shell: bash
        run: |
          cd web-ui
          npm install --package-lock-only --ignore-scripts
          npm ci
          npm run typecheck
          npm run build
      - name: Commit verified fix and remove temporary files
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/fix-v223.yml .github/scripts/fix_v223.py
          git config user.name 'Atingaii'
          git config user.email 'zyaiting@email.swu.edu.cn'
          git add -A
          git commit -m 'fix: force historical repricing and normalize official routes'
          git push origin HEAD:main
